node_modules/
dist/
# Exception: Dockerfile.local copies the host-prebuilt SPA from dist/host/browser, so it must
# stay in the build context. The rest of dist/ stays excluded.
!dist/host/browser
.nx/
.angular/
